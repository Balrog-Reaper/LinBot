# ═══════════════════════════════════════════
# LinBot 專案級 npm 設定
# ═══════════════════════════════════════════

# 鎖定精確版本（安裝時不加 ^ 或 ~）
# 避免不同開發者因為版本範圍差異導致「我的電腦可以跑」的問題
save-exact=true

# 指定 Node.js 引擎版本限制（配合 package.json 的 engines 欄位）
engine-strict=true
