# 1.把js 改成ts

# 2. 排程系統
<!-- // ═══════════════════════════════════════════
    // 以下為未來擴充預留區
    // ═══════════════════════════════════════════

    // 範例：每日早安問候（Phase 2）
    // agenda.define("daily_greeting", async (job) => { ... });

    // 範例：每週伺服器活躍度週報（Phase 2）
    // agenda.define("weekly_report", async (job) => { ... }); -->

# 3.github連結優化
<!-- 💡 小提醒：GitHub 的回傳訊息顯示您可能把遠端儲存庫改名為 LinBot.git 了，雖然現在推播還是能自動轉址成功，但您可以找時間在終端機輸入 git remote set-url origin https://github.com/Balrog-Reaper/LinBot.git 來更新本地的遠端連結，會比較標準喔！ -->